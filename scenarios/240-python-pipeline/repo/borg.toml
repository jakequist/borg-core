# A repo is a directory, whatever language its pipelines are written in. `borg repo push` reads this,
# asks each pipeline to describe itself, and emits the resulting def events.
[repo]
id = 1
name = "investing"

[[pipelines]]
command = "pipelines/is_investible.py"
